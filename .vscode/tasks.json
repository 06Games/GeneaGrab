{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-backend-only",
            "type": "shell",
            "command": "cargo build",
            "options": {
                "cwd": "${workspaceFolder}/backend/tauri"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
