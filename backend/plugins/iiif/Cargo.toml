[package]
name = "geneagrab-plugin-iiif"
version = "4.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
geneagrab-plugin-core = { path = "../../plugin_core", features = ["guest"] }
extism-pdk = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
