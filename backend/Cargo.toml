[workspace]
resolver = "2"
members = [
    "core",
    "migration",
    "providers/common",
    "providers/ad06",
    "providers/geneanet",
    "dates",
    "tauri",
]

[profile.release]
opt-level = 3
codegen-units = 16
lto = "thin"
strip = true

